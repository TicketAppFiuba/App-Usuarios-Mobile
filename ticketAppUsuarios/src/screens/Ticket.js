import React from 'react';
import {View, Text} from 'react-native';

export default function Ticket({navigation}) {
    return (
        <View>
            <Text>Ticket</Text>
        </View>
    );
}
